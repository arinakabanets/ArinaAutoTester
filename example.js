/**
 * Multiplies two values and prints result
 * @param {number} x 
 * @param {number} y 
 * @returns x * y
 */
function multiply(x, y) {
    let result = x * y;
    console.log(`Multiplying ${x} by ${y} = ${result}`);
    return result;
}

let x = 10;
let y = 6;
 
//console.log("Mulltiplying x by y = ");
//console.log("Multiplying " + x + " by " + y + " = ");

multiply(3, 7);